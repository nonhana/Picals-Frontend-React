import { FC } from 'react'
import LoginWindow from '@/components/login/login-window'

const Login: FC = () => {
  return (
    <div
      className='
        h-screen
      '>
      <LoginWindow />
    </div>
  )
}

export default Login
